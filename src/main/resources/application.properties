# Application
spring.application.name=pinceletas-user-auth-service
server.port=${PORT:8081}

# PostgreSQL Database
spring.datasource.url=${DATABASE_URL:jdbc:postgresql://localhost:5432/pinceletas}
spring.datasource.driver-class-name=org.postgresql.Driver
spring.datasource.username=${PGUSER:postgres}
spring.datasource.password=${PGPASSWORD:password}

# JPA
spring.jpa.show-sql=false
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.format_sql=false
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.defer-datasource-initialization=false

# SQL Initialization
spring.sql.init.mode=never

# Swagger/OpenAPI
springdoc.api-docs.path=/v3/api-docs
springdoc.swagger-ui.path=/swagger-ui.html
springdoc.swagger-ui.operationsSorter=method
springdoc.swagger-ui.tagsSorter=alpha
springdoc.swagger-ui.enabled=true
springdoc.api-docs.enabled=true
springdoc.cache.disabled=true

# Logs para produccion
logging.level.org.springframework.security=INFO
logging.level.org.springdoc=INFO
logging.level.org.springframework.amqp=INFO
logging.level.ar.edu.utn.frc.tup.tesis.pinceletas_user_auth_service.Rabbit=INFO

# Email configuration - Gmail
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${EMAIL_USERNAME:cbapinceletas@gmail.com}
spring.mail.password=${EMAIL_PASSWORD:ghimtfyvcmcivloc}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000

# Custom email
app.mail.from=${FROM_EMAIL:noreply@pinceletas.com}

# Location APIs configuration
app.location.api.countries-states.url=${COUNTRIES_STATES_API_URL:https://countriesnow.space/api/v0.1/countries}
app.location.api.rest-countries.url=${REST_COUNTRIES_API_URL:https://restcountries.com/v3.1}
app.location.cache.ttl=${LOCATION_CACHE_TTL:3600}
app.location.api.timeout=${LOCATION_API_TIMEOUT:10000}

# Spring cache configuration
spring.cache.cache-names=countries,country,states,cities
spring.cache.caffeine.spec=maximumSize=1000,expireAfterWrite=3600s

# Location service logs
logging.level.ar.edu.utn.frc.tup.tesis.pinceletas_user_auth_service.services.Impl.LocationServiceImpl=INFO
logging.level.ar.edu.utn.frc.tup.tesis.pinceletas_user_auth_service.controllers.LocationController=INFO

# Firebase Configuration - SEGURO CON VARIABLES DE ENTORNO
firebase.project-id=${FIREBASE_PROJECT_ID:login-4f76e}
firebase.credentials.json=${FIREBASE_CREDENTIALS_JSON:}

# JWT Configuration para tokens Firebase
app.firebase.jwt.secret=${FIREBASE_JWT_SECRET:dG9tYXNoZXJyYWRvaW5zdGl0dXRvYXRsZXRpY29jZW50cmFsY29yZG9iYTE5MTg=}
app.firebase.jwt.issuer=${FIREBASE_JWT_ISSUER:tesis-backend}
app.firebase.jwt.exp-minutes=${FIREBASE_JWT_EXP_MINUTES:60}

app.cors.allowed-origins=${FRONTEND_URL}

# RabbitMQ Config
spring.rabbitmq.addresses=${CLOUDAMQP_URL:amqp://localhost:5672}
spring.rabbitmq.username=${RABBITMQ_USERNAME:guest}
spring.rabbitmq.password=${RABBITMQ_PASSWORD:guest}
spring.rabbitmq.virtual-host=${RABBITMQ_VHOST:/}

app.rabbitmq.exchange=notificaciones.exchange
app.rabbitmq.routing-key=notificaciones.key

# Jackson configuration
spring.jackson.date-format=com.fasterxml.jackson.databind.util.ISO8601DateFormat
spring.jackson.serialization.write-dates-as-timestamps=false